generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PG_PRISMA_DATABASE_URL")
}

model Tag {
  id        Int      @id @default(autoincrement())
  tag       String   @unique
  usernames String[]
}
